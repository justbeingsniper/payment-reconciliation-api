# Copy to .env for local overrides. Everything has a sane default.

# Leave unset for zero-setup local SQLite (sqlite:///./setu.db).
# For Postgres:
# DATABASE_URL=postgresql+psycopg2://setu:setu@localhost:5432/setu

STUCK_THRESHOLD_HOURS=24
